plugin: aws_ec2

regions:
  - us-east-1

filters:
  "tag:aws:autoscaling:groupName": "*"

hostnames:
  - private-ip-address

keyed_groups:
  - key: tags.Name
    prefix: tag

compose:
  ansible_host: private_ip_address